ingress:
  enabled: true
  hostname: grafana.apatsev.org.ru
  annotations:
    kubernetes.io/ingress.class: traefik
    cert-manager.io/cluster-issuer: ca-issuer
  tls:
    - secretName: grafana-cert
      hosts:
        - grafana.apatsev.org.ru