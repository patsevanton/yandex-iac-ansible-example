---
- hosts: lokiindocker
  become: true
  tasks:

    - name: docker stop
      shell: "docker stop $(docker ps -a -q)"
      args:
        chdir: /etc/docker-compose

    - name: docker rm
      shell: "docker rm $(docker ps -a -q)"
      args:
        chdir: /etc/docker-compose
